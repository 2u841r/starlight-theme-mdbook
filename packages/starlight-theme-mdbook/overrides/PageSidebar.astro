{/* mdBook folds "on this page" headings into the left sidebar, nested under
    the active chapter link (see Sidebar.astro override), instead of a
    separate right-hand column. This override intentionally renders nothing
    so Starlight's default right TOC panel + mobile TOC bar disappear. */}
